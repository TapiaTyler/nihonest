# Source Review Records

Validated JSON records and derived Markdown reports created by `npm run sources:report` belong in this directory. Do not mark a record resolved or edit monitoring fingerprints by hand; use the approval-gated resolution command after human review.
