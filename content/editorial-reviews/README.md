# Editorial Approval Records

This directory stores immutable, validated approval records for exact content revisions marked `verified`. Generate records with `npm run editorial:record`; do not create or rewrite their JSON manually. Git history is the authoritative audit trail.
